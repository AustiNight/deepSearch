name = "deepsearch"
main = "workers/worker.ts"
compatibility_date = "2024-12-01"
compatibility_flags = ["nodejs_compat"]

# KV namespace binding for Cloudflare Access allowlist sync and universal settings.
# Replace the placeholder IDs below with real namespace IDs:
# `wrangler kv namespace create ACCESS_ALLOWLIST_KV` (and --preview for preview_id).
# `wrangler kv namespace create SETTINGS_KV` (and --preview for preview_id).
kv_namespaces = [
  { binding = "ACCESS_ALLOWLIST_KV", id = "d34711020d7843d0932ef8a10323bb68" },
  { binding = "SETTINGS_KV", id = "00000000000000000000000000000000" }
]
